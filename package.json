{
  "name": "water-monitor",
  "version": "1.0.0",
  "description": "Dashboard de monitoramento de nível de água via Arduino",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev:server": "node server.js",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client"
  },
  "dependencies": {
    "@serialport/parser-readline": "^12.0.0",
    "express": "^4.18.2",
    "serialport": "^12.0.0",
    "ws": "^8.16.0"
  }
}
